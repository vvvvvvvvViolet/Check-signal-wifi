[pytest]
testpaths = tests
pythonpath = ../
# Tests are function-based. Without this, pytest tries to collect the
# `TestRecord` model class purely because of its name.
python_classes =
filterwarnings =
    ignore::DeprecationWarning
